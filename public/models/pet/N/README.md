N (Normal) pet models

Place your v1/v2/v3 glb models here when you want to use local assets as fallbacks.
Expected filenames:
- v1.glb
- v2.glb
- v3.glb

These are used by the client when a `pet_masters.model_url` is not provided.